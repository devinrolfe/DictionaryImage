import sensitiveData from "../../sensitiveData";

const awsRekognitionInfo = {
    "accessKeyId": sensitiveData.accessKeyId,
    "secretAccessKey": sensitiveData.secretAccessKey,
    "region": "us-east-2"
};

export default awsRekognitionInfo;